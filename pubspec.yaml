name: farmfresh_flutter
description: "FarmFresh Hub — Florida's premier decentralized food network"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  google_fonts: ^6.2.1
  #lucide_icons: ^0.257.0  # using Material icons instead
  intl: ^0.19.0
  uuid: ^4.5.1
  flutter_animate: ^4.5.0
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
